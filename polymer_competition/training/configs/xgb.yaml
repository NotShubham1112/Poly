# XGBoost hyperparameters
xgb:
  n_estimators: 2000
  learning_rate: 0.05
  max_depth: 6
  subsample: 0.8
  colsample_bytree: 0.8
  reg_alpha: 0.0
  reg_lambda: 1.0
  early_stopping_rounds: 50
  tree_method: "hist"
  device: "cuda"
  eval_metric: "rmse"
